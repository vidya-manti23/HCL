VECTORS:

1) SRMU =SORT, REVERSE, MAX, MIN, UNIQUE
sort(v.begin(), v.end());
reverse(v.begin(), v.end());
*max_element(v.begin(), v.end());
*min_element(v.begin(), v.end());

2) ABCFF= ACUMMULATE, BINARY_SEARCH, COUNT, FILL, FIND
accumulate(v.begin(), v.end(), 0);
binary_search(v.begin(), v.end(), x);
count(v.begin(), v.end(), x);
find(v.begin(), v.end(), x);
fill(v.begin(), v.end(), 1);

3) E = arr.erase(unique(arr.begin(),arr.end(), v.end()); // replicas are in last, so we erase them
ABCFF= ACUMMULATE, BINARY_SEARCH, COUNT, FILL, FIND
